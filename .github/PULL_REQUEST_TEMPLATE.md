## What this does

<!-- Summary of the change and why it's needed. Link any related issue. -->

## Checklist

- [ ] `cargo build --release` passes
- [ ] `cargo test` passes
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` passes
- [ ] `cargo fmt` applied
- [ ] If this touches a SQL driver adapter: noted which live-DB integration tests (Postgres/MySQL/MSSQL/Oracle) were run locally, if any
- [ ] If this changes a config format or CLI behavior: docs/README updated
