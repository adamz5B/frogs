---
name: Bug report
about: Something in frogs isn't behaving as documented
title: ""
labels: bug
---

**Describe the bug**
A clear description of what's wrong.

**To Reproduce**
Minimal `openapi.yaml` / endpoint mapping / config needed to trigger it, and the exact `frogs` command run.

**Expected behavior**
What you expected to happen instead.

**Environment**
- `frogs` version/commit:
- OS:
- Build (`cargo build --release` flags, e.g. `--no-default-features`, `--features mysql`):

**Logs / output**
Relevant CLI output, `config/errors.discovered.json` entries, or panic backtrace, if any.
